apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: said-praxis-form-tls
  namespace: {{ .Release.Namespace }}
spec:
  secretName: said-praxis-form-tls
  dnsNames:
    - said.praxis-form.de
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: praxis-form-letsencrypt-prod
  usages:
    - digital signature
    - key encipherment
